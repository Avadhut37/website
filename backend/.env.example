# Application
APP_NAME=iStudiox
APP_VERSION=0.1.0
APP_URL=http://localhost:8000
DEBUG=true

# Security - CHANGE IN PRODUCTION!
SECRET_KEY=your-super-secret-key-min-32-characters-long
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Database
DATABASE_URL=sqlite:///./data/db.sqlite
# For PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/istudiox

# Redis (for rate limiting and background jobs)
REDIS_URL=redis://localhost:6379/0

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================================
# AI PROVIDERS - Multi-Provider Architecture for FREE Scaling
# ============================================================
# 
# Provider Priority:
# 1. Gemini (PRIMARY)  - Best quality, 1500 free/day
# 2. Groq (CODE)       - Fastest, 14400 free/day, best for code
# 3. DeepSeek (REASON) - Best reasoning, limited free credits
# 4. OpenRouter (BACKUP) - Multiple models, 50 free/day
#
# Task-Based Routing:
# - CODE tasks → Groq (fastest)
# - REASONING tasks → DeepSeek (if available) or OpenRouter
# - UI/TEXT tasks → Gemini (best quality)
# ============================================================

# AI Providers - Gemini (PRIMARY - 1500 free/day, best quality)
# Get FREE key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# AI Providers - Groq (CODE specialist - 14400 free/day, FASTEST!)
# Get FREE key at: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile

# AI Providers - DeepSeek (REASONING specialist - limited free credits)
# Get FREE key at: https://platform.deepseek.com/api_keys
# Note: Free credits run out, then $0.14/1M tokens (very cheap)
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_MODEL=deepseek-coder

# AI Providers - OpenRouter (BACKUP - 50 free/day, 7+ free models)
# Get FREE key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_MODEL=meta-llama/llama-3.2-3b-instruct:free
OPENROUTER_USE_FREE_ONLY=true

# LLM Settings
LLM_TIMEOUT=120
LLM_MAX_TOKENS=8192

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60
RATE_LIMIT_GENERATION=10/minute

# Generation
MAX_SPEC_LENGTH=50000
MAX_PROJECT_NAME_LENGTH=100
WORK_DIR=./work
